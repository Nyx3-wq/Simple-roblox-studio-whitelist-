const config = {
    // The bot token
    token: "",
    
    // Role ID for users who can blacklist others
    blacklistRoleId: "",
    
    // Role ID for users who can use the 'gen' command
    whitelistedRoleId: ""
  };
  
  export default config;
  